<% if type == :index%>
<li>
<% end %>
<table>
    <thead>
        <tr>
            <th colspan="2">Insert Image Here</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>Name</td>
            <td><%= cat.name %></td>
        </tr>
        <tr>
            <td>Birthday</td>
            <td><%= cat.birth_date %></td>
        </tr>
        <tr>
            <td>Gender</td>
            <td><%= cat.sex %></td>
        </tr>
        <tr>
            <td>Color</td>
            <td><%= cat.color %></td>
        </tr>
        <tr>
            <td>About Me</td>
            <td><%= cat.description %></td>
        </tr>
    </tbody>
</table>

<% if type == :index%>
  <a href="<%=cat_url(cat)%>">View cat</a> 
  </li>
<% else %>
  <a href="<%=cats_url%>">Back to Home</a> 
<% end %>
<br>


 